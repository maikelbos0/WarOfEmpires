@model WarOfEmpires.Models.Empires.ResourceHeaderViewModel

<div class="d-flex flex-row justify-content-end">
    <div class="p-2">@Model.Resources.Gold.ToString(StringFormat.Integer) <span class="si si-gold"></span></div>
    <div class="p-2">@Model.Resources.Food.ToString(StringFormat.Integer) <span class="si si-food"></span></div>
    <div class="p-2">@Model.Resources.Wood.ToString(StringFormat.Integer) <span class="si si-wood"></span></div>
    <div class="p-2">@Model.Resources.Stone.ToString(StringFormat.Integer) <span class="si si-stone"></span></div>
    <div class="p-2">@Model.Resources.Ore.ToString(StringFormat.Integer) <span class="si si-ore"></span></div>
    <div class="p-2">@Model.AttackTurns.ToString(StringFormat.Integer) <span class="si si-attack"></span></div>
    <div class="p-2">@Model.BankTurns.ToString(StringFormat.Integer) <span class="si si-bank"></span></div>
</div>