@{
    ViewBag.Title = "Bank buildings";
}

<h2>Bank buildings</h2>

<p>
    Here you can upgrade your gold and resource banks to hide your resources from enemies.
</p>

<building-totals/>

<div class="row">
    <div class="col-12 col-md-6 col-lg-4 partial-content" data-partial-url="@Url.Action("_Building", "Empire", new { BuildingType = "GoldBank" })">
    </div>

    <div class="col-12 col-md-6 col-lg-4 partial-content" data-partial-url="@Url.Action("_Building", "Empire", new { BuildingType = "FoodBank" })">
    </div>

    <div class="col-12 col-md-6 col-lg-4 partial-content" data-partial-url="@Url.Action("_Building", "Empire", new { BuildingType = "WoodBank" })">
    </div>

    <div class="col-12 col-md-6 col-lg-4 partial-content" data-partial-url="@Url.Action("_Building", "Empire", new { BuildingType = "StoneBank" })">
    </div>

    <div class="col-12 col-md-6 col-lg-4 partial-content" data-partial-url="@Url.Action("_Building", "Empire", new { BuildingType = "OreBank" })">
    </div>
</div>