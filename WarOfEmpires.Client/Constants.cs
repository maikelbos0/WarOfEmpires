namespace WarOfEmpires.Client;

public static class Constants
{
    public const string UnauthenticatedClient = nameof(UnauthenticatedClient);
    public const string AuthenticatedClient = nameof(AuthenticatedClient);
    public const string AuthenticationScheme = "Bearer";
    public const string Tokens = nameof(Tokens);
}
